<!--holding.wxml - 持仓页面-->
<wxs module="utils">
var toFixed = function(value, digits) {
  var num = parseFloat(value);
  if (isNaN(num)) {
    return '0.00';
  }
  return num.toFixed(digits || 2);
}

module.exports = {
  toFixed: toFixed
}
</wxs>

<scroll-view 
  scroll-y 
  class="container" 
  refresher-enabled 
  refresher-triggered="{{refreshing}}" 
  bindrefresherrefresh="onRefresh"
>
  <view class="summary">
    <view class="summary-title">持仓总金额（按已确认净值）</view>
    <view class="summary-value">¥{{utils.toFixed(totalValue, 2)}}</view>
    <view class="summary-profit-row">
      <view class="summary-profit {{totalDayProfit >= 0 ? 'positive' : 'negative'}}">
        今日估算 {{totalDayProfit >= 0 ? '+' : ''}}{{utils.toFixed(totalDayProfitRate, 2)}}%
      </view>
      <view class="summary-profit {{totalDayProfit >= 0 ? 'positive' : 'negative'}}">
        {{totalDayProfit >= 0 ? '+' : ''}}¥{{utils.toFixed(totalDayProfit, 2)}}
      </view>
    </view>
    <view wx:if="{{holdings.length > 0}}" class="data-source-bar">
      <text class="ds-status ds-{{marketStatus}}">{{statusLabel || '--'}}</text>
      <text wx:if="{{valuationTime}}" class="ds-item">估值 {{valuationTime}}</text>
      <text wx:if="{{valuationDate}}" class="ds-item">净值 {{valuationDate}}</text>
      <text class="ds-hint">· 估值仅为预测，盘后净值确认；带「自算」为重仓估算</text>
    </view>
    <view class="summary-actions">
      <button class="import-btn" bindtap="goToImport">导入持仓</button>
      <button class="calendar-btn" bindtap="goToCalendar">收益日历</button>
      <button class="clear-btn" bindtap="clearAllHoldings" wx:if="{{holdings.length > 0}}">清空</button>
    </view>
  </view>

  <view wx:if="{{loading}}" class="skeleton">
    <view class="sk-row" wx:for="{{[1,2,3,4,5]}}" wx:key="*this">
      <view class="sk-line sk-w60"></view>
      <view class="sk-line sk-w30"></view>
    </view>
  </view>

  <view wx:elif="{{holdings.length === 0}}" class="empty">
    <text>暂无持仓</text>
    <text class="empty-tip">在基金详情页添加持仓</text>
  </view>

  <view wx:else class="fund-list">
    <!-- 横向滚动多列表：首列(名称)粘性固定,右侧列可左右滑动 -->
    <scroll-view scroll-x class="tbl-scroll">
      <view class="tbl">
        <view class="trow thead">
          <view class="c c-name">名称/占比</view>
          <view class="c">今日</view>
          <view class="c c-board">关联板块</view>
          <view class="c">持有金额</view>
          <view class="c">持有收益</view>
          <view class="c">收益率</view>
          <view class="c">近1月</view>
          <view class="c">近1年</view>
        </view>
        <view wx:for="{{holdings}}" wx:key="code" class="trow" bindtap="goToDetail" data-code="{{item.code}}">
          <view class="c c-name">
            <text class="fund-name">{{item.name}}</text>
            <view class="fund-code">
              <text>{{item.code}}</text>
              <text wx:if="{{item.pendingAmount > 0 && !item.shares}}" class="pending-tag">待确认</text>
              <text wx:elif="{{item.pendingAmount > 0}}" class="pending-tag">加仓中</text>
              <text wx:else class="fund-ratio">{{utils.toFixed(item.holdingRatio, 2)}}%</text>
            </view>
          </view>
          <!-- 今日 -->
          <view class="c c-col">
            <block wx:if="{{item.pendingAmount > 0 && !item.shares}}"><text class="pending-text">确认中</text></block>
            <block wx:else>
              <text class="{{item.dayGrowth >= 0 ? 'positive' : 'negative'}}">{{item.dayGrowth !== 0 ? (item.dayGrowth >= 0 ? '+' : '') + utils.toFixed(item.dayGrowth, 2) + '%' : '-'}}</text>
              <text class="est-tag" wx:if="{{item.estimateSource === 'computed'}}">自算</text>
              <text class="est-tag est-tag-nav" wx:elif="{{item.estimateSource === 'none'}}">净值</text>
            </block>
          </view>
          <!-- 关联板块 -->
          <view class="c c-board c-col">
            <text class="board-name">{{item.board || '-'}}</text>
            <text wx:if="{{item.board}}" class="{{item.boardChange == null ? '' : (item.boardChange >= 0 ? 'positive' : 'negative')}}">{{item.boardChange == null ? '--' : (item.boardChange >= 0 ? '+' : '') + utils.toFixed(item.boardChange, 2) + '%'}}</text>
          </view>
          <!-- 持有金额 -->
          <view class="c">¥{{utils.toFixed(item.currentAmount || item.amount, 0)}}</view>
          <!-- 持有收益 -->
          <view class="c {{item.profit >= 0 ? 'positive' : 'negative'}}">{{item.profit >= 0 ? '+' : ''}}{{utils.toFixed(item.profit, 2)}}</view>
          <!-- 收益率 -->
          <view class="c {{item.profitRate >= 0 ? 'positive' : 'negative'}}">{{item.profitRate >= 0 ? '+' : ''}}{{utils.toFixed(item.profitRate, 2)}}%</view>
          <!-- 近1月/近1年 -->
          <view class="c {{item.m1 >= 0 ? 'positive' : 'negative'}}">{{item.m1 >= 0 ? '+' : ''}}{{utils.toFixed(item.m1, 2)}}%</view>
          <view class="c {{item.y1 >= 0 ? 'positive' : 'negative'}}">{{item.y1 >= 0 ? '+' : ''}}{{utils.toFixed(item.y1, 2)}}%</view>
        </view>
      </view>
    </scroll-view>
    <view class="slide-hint">← 左右滑动查看更多 · 金额/收益按确认净值</view>
  </view>
</scroll-view>
